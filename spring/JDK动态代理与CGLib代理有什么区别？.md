# JDK动态代理与CGLib代理有什么区别？

为什么要采用两种代理方式？

因为JDK本身只提供接口代理，而不支持类代理

**JDK动态代理**

​	允许在运行期间创建接口的代理实例

InvocationHandler

Proxy

**CGLIB动态代理**

​	CGLIB采用底层字节码技术，可以为一个类创建子类，在子类中采用方法拦截的技术拦截所有父类方法的调用并顺势织入横切逻辑

缺点：由于CGLIB采用动态创建子类的方法生成代理对象，所以不能对目标类中的final或private方法进行代理