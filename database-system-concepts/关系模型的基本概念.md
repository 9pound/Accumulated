# 关系模型的基本概念

#### 关系Relation

关系就是一张二维表，由行和列组成。二维表的表名就是关系名。

#### 元组Tuple

二维表中的行（row）称为元组。

#### 属性Attribute

二维表中的列（column）称为属性。

####  关键字key

可唯一标识元组的属性或属性集，也称为关系键或主码。

#### 域Domain

属性的取值范围。

#### 分量

每一行对应的列的属性值，即元组中的一个属性值。

#### 关系模式

对关系的描述，一般表示为：关系名（属性1，属性 2，属性3）。

# 关系模型的概述

####  关系数据模型的三个基本组成部分

#####  关系数据结构

##### 关系操作

1. 选择操作是指在关系中选择满足某些条件的元组（行）
2. 投影操作是在关系中选择若干属性列组成新的关系
3. 连接操作是将不同的两个关系连接成为一个关系

##### 关系中的完整性约束

###### 实体完整性

- 若属性A是基本关系R的主属性 ，则A不能取空值（主键不能为空）

###### 参照完整性

- 若属性F是基本关系R的外键它与基本关系S的主键K相对应（基本关系R和S不一定是不同的关系），则对于R中每个元组在F上的值必须为：
- 或者取空值
- 或者等于S中某个元组的主键值

###### 用户自定义完整性

- 就是针对某一具体关系数据库的唯一约束条件

## 2.2. 关系数据结构和基本术语

### 2.2.1. 候选键

关系中的某一属性组，能唯一的标识一个元组。

### 2.2.2. 主键

一列，其值能够唯一的区分表中的每个行，主键不能为NULL值

### 2.2.3. 主属性

### 2.2.4. 非主属性

### 2.2.5. 全键

### 2.2.6. 外键

一个属性“组”不是所在关系的主键，但是其他关系的主键，称未见

## 2.3. 关系的性质

（1）   列是同质的

（2）   不同列的数据可以来自同一个域

（3）   列的顺序无所谓

（4）   任意两个元组的后选键不能相同

（5）   行的顺序无所谓

（6）   每个分量必须取原子值，即每个分量都必须是不可分的数据项

# 3.  关系模式的规范化设计

## 3.1. 数据依赖

### 3.1.1. 定义

（1）   数据依赖是关系内部属性与属性之间的一种约束关系，这种约束关系通过属性间值的相等与否体现出来的数据间的相关联系

### 3.1.2. 分类

（1）   函数依赖

（2）   多值依赖

 

## 3.2. 规范化的关系模式

如果一个关系模式的所有属性都是基本的，不可分的。则R是第一范式

# 4.  数据库基础

## 4.1. 什么是数据库

数据库是一个以某种有组织的方式存储的数据集合，可以看成一个文件柜

## 4.2. 什么是表

表是一种结构化的文件，可用来存储某种特定类型的数据

## 4.3. 什么是模式

描述数据库和表的布局，及特性信息