# Optional

如何创建Optional

1、声明一个空的Optional

Optional<car> optcar = Optional.empty();



2、依据一个非空值创建Optional

Optional<Car> optCar = Oprional.of(car);



3、可接受null的Optional

Optional<car> optcar = Optional.ofNullabel(car);

 