### 动态绑定和静态绑定

**绑定**

- 将一个方法调用同一个方法主体关联起来称为绑定。

**静态绑定**

- 在程序执行前进行绑定（由编译器和连接器实现）。
- 如果是private方法，static方法，final方法或者是构造器，编译器可以准确地知道应该调用哪个方法。

**动态绑定**

- 在运行时根据对象的类型进行绑定。

- 调用的方法依赖于隐式参数的实际类型，并且在运行时动态绑定。

  （java编程思想 p151）